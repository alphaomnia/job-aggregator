# Pinned to exact versions for reproducible CI builds (was >= ranges, a
# supply-chain risk since CI has contents:write). Bump deliberately.
# jinja2 removed -- it was declared but never imported (templating is a plain
# file copy in main.py).
requests==2.34.2
beautifulsoup4==4.14.3
lxml==6.1.1
feedparser==6.0.12
PyYAML==6.0.3
python-dateutil==2.9.0.post0
